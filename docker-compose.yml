version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
    command: ["make","test"]
    image: devaa72/docker-project-74
#    platform: linux/amd64


